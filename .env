- name: Create .env file
  copy:
    dest: "{{ app_dest }}/.env"
    content: |
      DB_HOST={{ db_host }}
      DB_USER={{ db_user }}
      DB_PASSWORD={{ db_password }}
      DB_NAME={{ db_name }}
